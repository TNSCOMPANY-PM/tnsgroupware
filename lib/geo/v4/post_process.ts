/**
 * v4 post_process — v3 의 5룰 그대로 재사용.
 * (brand→브랜드 / 억단위 / percentile 자연어 / "→ 즉" 다양화 / 출처 압축)
 */

export { postProcess } from "../v3/post_process";
export type { PostProcessResult } from "../v3/post_process";
